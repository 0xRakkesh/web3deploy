{
  "name": "build-server",
  "version": "1.0.0",
  "description": "Build server that clones a repo, builds it, and uploads the output to S3",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "script.js",
  "scripts": {
    "start": "node script.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "dotenv": "^16.4.5",
    "ioredis": "^5.3.2",
    "mime-types": "^3.0.2"
  }
}
